# Example environment configuration for Cimeika
# Replace the values below with your own credentials

<<<<<<< HEAD
# Required API key for OpenWeather data
OPENWEATHER_KEY=your_openweather_api_key

# Required API key for Free Astrology API
ASTROLOGY_KEY=your_astrology_api_key

# Optional: Port for the Express server (defaults to 3000)
# PORT=3000

# Optional: Hugging Face Inference API token
# HUGGINGFACE_TOKEN=your_hf_api_token

# Optional: Base URL for the API scenario script
# BASE_URL=http://localhost:3000
=======
# Required API key for OpenAI
OPENAI_API_KEY=you openai API key

# Required write token for Hugging Face deployment
HF_WRITE_TOKEN=your_hf_api_token

# Optional: Port for the Express server (defaults to 7860)
# PORT=7860

# Hugging Face Inference API token required for /ai/huggingface/completion endpoint
# scripts/api_scenario.js will skip that request if this isn't set
HUGGINGFACE_TOKEN=your_hf_api_token

# Optional: override the default Hugging Face Space URL used by the proxy endpoint
# HF_SPACE_URL=https://ihorog-cimeika-api.hf.space

# Optional: override defaults used by weather and astrology endpoints
# DEFAULT_CITY=London
# DEFAULT_SIGN=aries

# Optional: Base URL for the API scenario script
# BASE_URL=http://localhost:7860
>>>>>>> origin/main
